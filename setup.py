from setuptools import setup, find_packages

setup(
    name='ttools',
    version='0.2.9',
    packages=find_packages(),
    install_requires=[
        'vectorbtpro',
        # list your dependencies here
    ],
) #find_packages()