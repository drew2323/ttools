from setuptools import setup, find_packages

setup(
    name='ttools',
    version='0.1.5',
    packages=['ttools'],
    install_requires=[
        'vectorbtpro',
        # list your dependencies here
    ],
) #find_packages()